{
  "scripts": {
    "dev": "yarn build && node index.js",
    "build": "rimraf build && tsup",
    "push": "yarn build && space push"
  },
  "dependencies": {
    "@shelter-zone/sz-express-utils": "^0.3.0",
    "body-parser": "^1.20.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^6.9.2",
    "superstruct": "^1.0.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.14",
    "@types/node": "^18.8.3",
    "cross-env": "^7.0.3",
    "rimraf": "^3.0.2",
    "tsup": "^6.4.0",
    "typescript": "^4.8.4"
  }
}
